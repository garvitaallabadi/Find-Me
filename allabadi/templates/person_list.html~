<h1>Persons</h1>

<ul>
  {% for person in object_list %}
    <li class="Uid">{{ person.uid }}
     {{ person.mac_hash }} {{person.rollno}} {{person.batch}} {{person.access}} {{person.email}} </li>
  {% endfor %}
</ul>

<a href="/logout">Sign Out</a>


